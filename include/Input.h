#ifndef INPUT_H
#define INPUT_H

class Input {
	public:
		static bool	aDown;
		static bool	aPressed;
		static bool	aReleased;
		static bool	bDown;
		static bool	bPressed;
		static bool	bReleased;
		static bool	cDown;
		static bool	cPressed;
		static bool	cReleased;
		static bool	dDown;
		static bool	dPressed;
		static bool	dReleased;
		static bool	eDown;
		static bool	ePressed;
		static bool	eReleased;
		static bool	fDown;
		static bool	fPressed;
		static bool	fReleased;
		static bool	gDown;
		static bool	gPressed;
		static bool	gReleased;
		static bool	hDown;
		static bool	hPressed;
		static bool	hReleased;
		static bool	iDown;
		static bool	iPressed;
		static bool	iReleased;
		static bool	jDown;
		static bool	jPressed;
		static bool	jReleased;
		static bool	kDown;
		static bool	kPressed;
		static bool	kReleased;
		static bool	lDown;
		static bool	lPressed;
		static bool	lReleased;
		static bool	mDown;
		static bool	mPressed;
		static bool	mReleased;
		static bool	nDown;
		static bool	nPressed;
		static bool	nReleased;
		static bool	oDown;
		static bool	oPressed;
		static bool	oReleased;
		static bool	pDown;
		static bool	pPressed;
		static bool	pReleased;
		static bool	qDown;
		static bool	qPressed;
		static bool	qReleased;
		static bool	rDown;
		static bool	rPressed;
		static bool	rReleased;
		static bool	sDown;
		static bool	sPressed;
		static bool	sReleased;
		static bool	tDown;
		static bool	tPressed;
		static bool	tReleased;
		static bool	uDown;
		static bool	uPressed;
		static bool	uReleased;
		static bool	vDown;
		static bool	vPressed;
		static bool	vReleased;
		static bool	wDown;
		static bool	wPressed;
		static bool	wReleased;
		static bool	xDown;
		static bool	xPressed;
		static bool	xReleased;
		static bool	yDown;
		static bool	yPressed;
		static bool	yReleased;
		static bool	zDown;
		static bool	zPressed;
		static bool	zReleased;

		static bool	downDown;
		static bool	leftDown;
		static bool	rightDown;
		static bool	upDown;

		static bool endDown;
		static bool endPressed;
		static bool endReleased;
		static bool homeDown;
		static bool homePressed;
		static bool homeReleased;
		static bool insDown;
		static bool insPressed;
		static bool insReleased;
		static bool pgdownDown;
		static bool pgdownPressed;
		static bool pgdownReleased;
		static bool pgupDown;
		static bool pgupPressed;
		static bool pgupReleased;

		static bool f1Down;
		static bool f1Pressed;
		static bool f1Released;
		static bool f2Down;
		static bool f2Pressed;
		static bool f2Released;
		static bool f3Down;
		static bool f3Pressed;
		static bool f3Released;
		static bool f4Down;
		static bool f4Pressed;
		static bool f4Released;
		static bool f5Down;
		static bool f5Pressed;
		static bool f5Released;
		static bool f6Down;
		static bool f6Pressed;
		static bool f6Released;
		static bool f7Down;
		static bool f7Pressed;
		static bool f7Released;
		static bool f8Down;
		static bool f8Pressed;
		static bool f8Released;
		static bool f9Down;
		static bool f9Pressed;
		static bool f9Released;
		static bool f10Down;
		static bool f10Pressed;
		static bool f10Released;
		static bool f11Down;
		static bool f11Pressed;
		static bool f11Released;
		static bool f12Down;
		static bool f12Pressed;
		static bool f12Released;

		static bool	escDown;
		static bool	escPressed;
		static bool	escReleased;

		Input();
		virtual ~Input();
	protected:
	private:
};
#endif // INPUT_H
